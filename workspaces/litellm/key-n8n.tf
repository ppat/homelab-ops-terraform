# n8n: BLOCKED, not yet written — same reason as key-openclaw.tf. This key needs 5 explicit
# models and its own bespoke mcp_tool_permissions across the same 5 servers (context7,
# playwright, grafana, home_assistant, github), narrower than openclaw's on grafana and github
# specifically per the live-key-inventory — but again only described there ("read-mostly"), not
# listed verbatim. Needed from the owner: the 5 model names, and each of the 5 servers' complete
# tool list, exactly as granted today. See key-openclaw.tf for the full reasoning.
